<UserControl x:Class="WloCharacterViewer.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="#cdd6f4">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Equipment -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Equipment" FontSize="14" FontWeight="Bold" Foreground="#cba6f7" Margin="0,0,0,8" />
            <ItemsControl ItemsSource="{Binding Inventory.Equipment}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#313244" CornerRadius="4" Padding="8" Margin="4" MinWidth="80">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="{Binding Slot, StringFormat=Slot {0}}" FontSize="10" Foreground="#6c7086" />
                                <TextBlock Text="{Binding ItemId}" FontWeight="Bold" Foreground="#f9e2af" FontSize="14" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- Bag -->
        <StackPanel Grid.Row="2" Margin="0,0,0,4">
            <TextBlock Text="Bag" FontSize="14" FontWeight="Bold" Foreground="#cba6f7" Margin="0,0,0,8" />
        </StackPanel>
        <Border Grid.Row="1" Background="#11111b" CornerRadius="6" Padding="4">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Inventory.Bag}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#313244" CornerRadius="4" Padding="6" Margin="3" Width="80" Height="60">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding ItemId}" FontWeight="Bold" Foreground="#a6e3a1" FontSize="13" HorizontalAlignment="Center" />
                                    <TextBlock HorizontalAlignment="Center" FontSize="10" Foreground="#6c7086">
                                        <Run Text="x" /><Run Text="{Binding Quantity, Mode=OneWay}" />
                                        <Run Text=" [" /><Run Text="{Binding Slot, Mode=OneWay}" /><Run Text="]" />
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
