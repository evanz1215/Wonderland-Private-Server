<UserControl x:Class="WonderlandServerWpf.Views.UpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="更新管理" FontSize="22" FontWeight="Bold" />
            <TextBlock Text="應用程式與地圖更新狀態" FontSize="13"
                       Foreground="{DynamicResource FgMutedBrush}" Margin="0,4,0,0" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Application Updates -->
            <GroupBox Header="應用程式更新" Grid.Column="0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding ApplicationUpdatesInfo}"
                               TextWrapping="Wrap" FontSize="13"
                               Foreground="{DynamicResource FgSecondaryBrush}" />
                </ScrollViewer>
            </GroupBox>

            <!-- Map Updates -->
            <GroupBox Header="地圖更新" Grid.Column="2">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding MapUpdatesInfo}"
                               TextWrapping="Wrap" FontSize="13"
                               Foreground="{DynamicResource FgSecondaryBrush}" />
                </ScrollViewer>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
