<UserControl x:Class="WonderlandServerWpf.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="伺服器設定" FontSize="22" FontWeight="Bold" />
            <TextBlock Text="排程更新與資料庫欄位對應" FontSize="13"
                       Foreground="{DynamicResource FgMutedBrush}" Margin="0,4,0,0" />
        </StackPanel>

        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTab}">
            <!-- Update Config -->
            <TabItem Header="排程更新">
                <StackPanel MaxWidth="500" HorizontalAlignment="Left" Margin="0,16,0,0">
                    <GroupBox Header="自動更新設定">
                        <StackPanel>
                            <CheckBox Content="啟用應用程式排程更新"
                                      IsChecked="{Binding EnableScheduledUpdate}" Margin="0,0,0,12" />
                            <CheckBox Content="更新前顯示警告通知"
                                      IsChecked="{Binding WarnOfUpdate}" />
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </TabItem>

            <!-- Database Column Linking -->
            <TabItem Header="資料庫欄位對應">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
                    <StackPanel MaxWidth="700" HorizontalAlignment="Left">
                        <GroupBox Header="User 資料表欄位對應">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="40" />
                                    <RowDefinition Height="40" />
                                    <RowDefinition Height="40" />
                                    <RowDefinition Height="40" />
                                    <RowDefinition Height="40" />
                                    <RowDefinition Height="40" />
                                    <RowDefinition Height="40" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="Table Name" Grid.Row="0" VerticalAlignment="Center"
                                           Foreground="{DynamicResource FgSecondaryBrush}" />
                                <TextBox Text="{Binding TableName, UpdateSourceTrigger=PropertyChanged}"
                                         Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" />

                                <TextBlock Text="Username" Grid.Row="1" VerticalAlignment="Center"
                                           Foreground="{DynamicResource FgSecondaryBrush}" />
                                <TextBox Text="{Binding UsernameRef, UpdateSourceTrigger=PropertyChanged}"
                                         Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" />

                                <TextBlock Text="Password" Grid.Row="2" VerticalAlignment="Center"
                                           Foreground="{DynamicResource FgSecondaryBrush}" />
                                <TextBox Text="{Binding PasswordRef, UpdateSourceTrigger=PropertyChanged}"
                                         Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" />

                                <TextBlock Text="DataBase ID" Grid.Row="3" VerticalAlignment="Center"
                                           Foreground="{DynamicResource FgSecondaryBrush}" />
                                <TextBox Text="{Binding UserIdRef, UpdateSourceTrigger=PropertyChanged}"
                                         Grid.Row="3" Grid.Column="1" VerticalAlignment="Center" />

                                <TextBlock Text="PassVerification" Grid.Row="4" VerticalAlignment="Center"
                                           Foreground="{DynamicResource FgSecondaryBrush}" />
                                <ComboBox ItemsSource="{Binding PassVerificationOptions}"
                                          SelectedIndex="{Binding PassVerification}"
                                          Grid.Row="4" Grid.Column="1" VerticalAlignment="Center" />

                                <TextBlock Text="Char Delete Code" Grid.Row="5" VerticalAlignment="Center"
                                           Foreground="{DynamicResource FgSecondaryBrush}" />
                                <TextBox Text="{Binding CharDeleteCodeRef, UpdateSourceTrigger=PropertyChanged}"
                                         Grid.Row="5" Grid.Column="1" VerticalAlignment="Center" />

                                <TextBlock Text="IM" Grid.Row="6" VerticalAlignment="Center"
                                           Foreground="{DynamicResource FgSecondaryBrush}" />
                                <TextBox Text="{Binding ImRef, UpdateSourceTrigger=PropertyChanged}"
                                         Grid.Row="6" Grid.Column="1" VerticalAlignment="Center" />
                            </Grid>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
